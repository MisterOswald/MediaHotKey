# Core
customtkinter>=5.2.0      # modern dark-themed UI
spotipy>=2.23.0           # Spotify Web API client
keyboard>=0.13.5          # global hotkeys
requests>=2.28.0          # Discord webhooks + HTTP

# Windows-only "Media mode" (universal SMTC transport control).
# Safe to skip on macOS/Linux — the app detects it and disables media mode.
winsdk>=1.0.0b10; sys_platform == "win32"

# Optional: minimize-to-tray support
pystray>=0.19.0
Pillow>=9.0.0

# Optional: build a standalone .exe (see build_exe.bat)
# pyinstaller>=6.0.0
